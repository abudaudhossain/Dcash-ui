import React from "react";
import { Outlet } from "react-router-dom";

const Account = () => {
    return (
        <section>
            <Outlet/>
        </section>
    );
}


export default Account;